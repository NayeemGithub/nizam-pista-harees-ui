import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'nizam-pista-harees-ionic',
  webDir: 'www'
};

export default config;
